Always:
1. For each "db_write":
    a. some "instance_delete_check" has control flow influence on "db_write"
    and
    b. some "instance_ban_check" has control flow influence on "db_write"
and
2. For each "db_read" that is not a db_user_read:
    a. some "instance_delete_check" has control flow influence on "db_read"
    and
    b. some "instance_ban_check" has control flow influence on "db_read"